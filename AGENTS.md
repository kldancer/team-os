# Personal Team OS 执行内核

本文件只定义跨项目稳定的团队协作底线和 Team OS 仓库维护规则。具体项目的业务事实、机器计划、Gate、生产边界与提交规则由项目自己的 `AGENTS.md` 和权威文档定义。

## 1. 权威与安全

1. 用户当前明确目标和授权最高；系统安全限制不得被任何本地文档放宽。
2. 项目仓库拥有项目领域事实。通用规则与项目合同冲突时，安全取更严格边界，项目事实取更具体且可验证的权威。
3. 不覆盖用户改动，不执行破坏性 Git 操作，不把无关变更带入任务。
4. 未经明确授权，不 stage、commit、push，不做远端写、生产写、数据删除或外部消息发送。
5. Key、Token、登录态、真实 Session ID、Transcript 和一次性日志不得进入 Team OS、Prompt 模板、截图或长期文档。

## 2. 协作内核

- 每个结果只有一个明确负责人；专家是按需角色，不是默认常驻进程。
- 先写清目标、非目标、读写集合、验收、预算与停止条件，再决定单 Agent 还是协作。
- 默认使用单 Agent。只有互补证据、真实可分解工作或高风险独立验证能够覆盖协调成本时才组队。
- 只有任务真正独立、写集合互斥且能分别验收时才并行；需要方案多样性时先独立首轮，再由负责人一次综合。
- 专业判断权随当前事实和能力移动，但写入责任始终单一；验证者不得通过修改实现制造通过结果。
- 软件交付默认由一个端到端 Feature Owner 对用户结果负责；前端、后端、UI/UX、数据、安全和 SRE 是按需能力，不是强制接力岗位。
- 交接传递结论、决策、变更集合、验证和证据引用，不复制完整对话。
- 重要派工采用闭环确认：接收者回传自己的理解、输出位置和完成条件；普通状态不持续广播。
- 工作状态使用 `intake → ready → doing → verifying → waiting → done`；`waiting` 必须写明等待对象和恢复条件。
- 回答、解释、评审默认只读；实现、外部写和生产操作必须由当前任务授权覆盖。

## 3. 上下文与记忆

- 通用短内核始终最小化；角色、工作流、项目文档和 Skill 按任务命中加载。
- Provider Session 保存短期对话连续性；稳定角色职责与跨项目方法进入本仓库；项目事实留在项目仓库。
- 只有经验证且预计复用的结论才进入长期文档。动态证据放在 `.work/` 或项目指定证据目录。

## 4. Team OS 维护

- 新规则先在真实项目中验证；只有被两个以上不同项目稳定复用且语义一致时才上移。
- 项目适配器只引用权威路径，不复制项目文档，不因登记而获得写权限。
- 本仓库不充当任务数据库、运行日志仓库或秘密存储；Munder 运行时默认只读加载。
- 模型名称、价格和排名不是长期角色合同；模型进入默认路由前必须同时通过能力、运行集成和代表性 eval。
