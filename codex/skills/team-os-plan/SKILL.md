---
name: team-os-plan
description: 将已经讨论清楚的想法收敛为一个可验收结果，并选择最小 Codex 协作拓扑。用于用户说“按结论开始推进”、复杂或跨仓实施、需要 DAG/角色/Session 分配，或任务边界尚未机器化时。它只提供通用规划叠加层；项目已有流程 Skill 时由项目 Skill 继续拥有交付流程。
---

# Team OS 结果规划

## 先建立一个结果

从当前对话提炼并在工作中持续维护：

- `outcome`：用户最终能直接验证的结果；
- `nonGoals`：本次明确不做的事情；
- `authority`：项目 `AGENTS.md`、规范、正式设计和机器入口；
- `scope`：读集合、写集合、明确排除项；
- `acceptance` 与适用验证；
- 目标时间、hard stop 和失败后的可证伪切换条件；
- 一个结果负责人。

不要要求用户重新填写已经在对话里给出的信息。普通 Gate、重试、波次或专家输入是同一结果的事件/子任务，不新建顶层结果。

Goal 只在用户明确要求时创建，并且一个 outcome 只对应一个；它用于长任务持续推进，不证明规划完整，也不因 plan revision、返工或刷新重复创建。

先判定任务意图：已有症状的排查或“定位并修复”是同一个可证伪诊断闭环；新模块仅设计/规划与结论已定后实施不同。讨论已充分且随后实施时，优先维护一份含决策基线、场景/失败面、DAG 和验收映射的实施规划，不机械复制一份结论记录再二次翻译。需要新建时以 [`references/module-implementation-plan.md`](references/module-implementation-plan.md) 为最小骨架；项目若有更具体模板或要求 program 规划文档，使用项目合同并由其机器入口绑定原文和指纹。

## 再选最小拓扑

默认 `solo`。只有满足下列之一才增加协作任务：

- 不同事实源需要独立首轮，且会产出可比较的独有证据；
- 子结果能独立验收且写集合互斥；
- 高风险候选需要不修改实现的独立验证者。

复杂模块、跨服务结果或两个以上独立证据域在实施前必须向用户显式给出一次拓扑判断，即使结论仍是 `solo`。协作任务是侧栏可见、拥有独立 Session、能单独继续和验收的真实 Codex 任务；不得把隐藏子智能体当作会话协同。当前主任务负责综合；用户选择 GPT-5.6 主链时，独立机械核验优先 Luna `low`，边界明确的局部设计/实现优先 Luna `medium`，高风险独立复核才提高档位。

需要协作时，为每个真实独立任务声明：目标、非目标、输入、写集合、禁止修改、验证、时间预算、完成条件和停止条件。先画依赖 DAG，再并发；写集合重叠则串行或保持单一写者。当前主任务负责综合和跨 Lane Gate。

大模块按用户可运行的纵向场景切片，不按前端/后端/UI 目录机械分包。规划完成的最低条件是：每个验收/场景都有实现 owner 与证据，每个跨边界失败都有拒绝、恢复或明确非目标；未决业务问题必须阻塞实施，不能留到 refresh 后逐个发现。同一 outcome 的遗漏沿用原 task ID，先在本地合并闭合再冻结一个候选。

只有需要显式选择专业角色或独立任务时，才读取 [`references/capabilities.yaml`](references/capabilities.yaml)。从中选择最少的稳定 `role.id` 和 `capabilityProfile.id`；简单 `solo` 任务不读取目录，也不因角色名称创建额外任务。该文件是安装时从 Team OS 权威目录生成的只读投影，本次任务授权仍由项目合同决定。

## 路由到项目流程

读取当前目录的项目指令。若项目提供 `design`、`deliver-change`、`diagnose`、`review`、`ship-changes` 或机器 planner，只选一个流程所有者并把结果合同交给它；本 Skill 不建立第二套任务数据库、不替代项目 Gate、不扩大授权。项目机器任务支持稳定 task ID 时，重规划沿用同一 task ID，不把 Plan、重试或波次升级为新结果。

## 完成

只有 outcome、适用验证、必要运行事实和已知风险披露闭合才完成。动态收据写项目指定 `.work`；长期合同只进入项目或 Team OS 的唯一权威位置。
